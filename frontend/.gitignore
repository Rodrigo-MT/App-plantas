# 🌿 ARQUIVOS .env VERSIONADOS INTENCIONALMENTE
# =============================================
# Este é um gerenciador de plantas para uso doméstico/educacional
# Não contém chaves de API, tokens ou dados sensíveis de produção
# As variáveis são apenas URLs de desenvolvimento e configurações básicas
# Versionamos .env para facilitar o setup de novos desenvolvedores
#
# ⚠️  EM CASO DE USO COM DADOS SENSÍVEIS:
# - Adicione .env no .gitignore
# - Use .env.example como template
# - Nunca commit credenciais reais

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# ARQUIVOS .env removidos.
# Estes DEVEM ser ignorados:
.env*.local
.env.production
.env.staging

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# 📁 BACKEND (se estiver no mesmo repositório)
backend/node_modules/
backend/dist/
backend/.env
backend/*.log

# 🗄️  Banco de dados (desenvolvimento)
*.sqlite
*.db